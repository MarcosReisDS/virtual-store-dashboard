export interface UserType {
    id?: number
    name: string
    mail: string
    password: string
}

export interface ProductType {
    id?: number
    name: string
    amounts: string
    value: string
    activated: boolean
}